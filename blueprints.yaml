name: Michael's Blog
version: 1
description: Das hier ist das Theme, dass ich für meinen Blog einsetze.
icon: bold
author:
  name: Michael
  email: michael.wasserfallen@vtg.admin.ch
keywords: bootstrap, theme, 

dependencies:
  - bootstrapper

form:
  validation: strict
  fields:
    dropdown.enabled:
        type: toggle
        label: Dropdown in navbar
        highlight: 1
        default: 1
        options:
          1: Enabled
          0: Disabled
        validate:
          type: bool
